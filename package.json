{
  "name": "flowx",
  "version": "1.0.0",
  "description": "FlowX - Intelligent Urban Mobility & Traffic Distribution Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"FRONTEND,BACKEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix frontend\" \"npm start --prefix backend\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm start --prefix backend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
